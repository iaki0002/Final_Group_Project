<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalgroupproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".car.CarChargerPhoneFragActivity"></activity>
        <activity android:name=".car.CarChargerFinderActivity" />
        <activity android:name=".currency.CurrencyFavouriteActivity" />
        <activity android:name=".currency.MainActivity_currency" />
        <activity android:name=".currency.test" /> <!-- Currency -->
        <activity android:name=".recipe.RecipeWebViewActivity" />
        <activity android:name=".recipe.RecipeFavoriteActivity" />
        <activity android:name=".recipe.MainRecipeActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>